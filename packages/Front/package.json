{
  "name": "front-consify",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "serve dist/public",
    "build": "tsc && yarn copy-files",
    "copy-files": "cp -r ./src/public/ ./dist/"
  },
  "dependencies": {
    "shared-consify": "1.0.0",
    "vue": "^3.2.20"
  },
  "repository": "git+https://github.com/anouarattn/consify.git"
}
